<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
  <div class="mb-4">
    <%= link_to '← 1:1 équipe', manager_one_on_ones_path,
          class: 'text-sm text-muted-soft hover:text-text-deep dark:text-white/50 dark:hover:text-muted-soft' %>
  </div>

  <!-- Header card -->
  <div class="bg-surface dark:bg-[#1C1B2E] rounded-lg shadow-sm border border-border-soft dark:border-white/10 p-6 mb-6">
    <div class="flex justify-between items-start">
      <div class="flex-1">
        <h1 class="text-2xl font-serif text-text-deep dark:text-white">
          1:1 avec <%= @one_on_one.employee.full_name %>
        </h1>
        <p class="text-sm text-muted-soft dark:text-white/50 mt-1">
          <%= l(@one_on_one.scheduled_at, format: :long) %>
        </p>
        <%
          status_classes = {
            'scheduled'  => 'bg-primary/10 dark:bg-primary/10/40 text-primary dark:text-primary border-blue-200 dark:border-blue-700',
            'completed'  => 'bg-green-100 dark:bg-green-900/40 text-green-800 dark:text-green-300 border-green-200 dark:border-green-700',
            'cancelled'  => 'bg-bg-soft dark:bg-surface/10 text-muted-soft dark:text-white/50 border-border-soft dark:border-white/15'
          }
          status_labels = { 'scheduled' => 'Planifié', 'completed' => 'Complété', 'cancelled' => 'Annulé' }
        %>
        <span class="mt-2 inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border <%= status_classes[@one_on_one.status] || 'bg-bg-soft text-muted-soft' %>">
          <%= status_labels[@one_on_one.status] || @one_on_one.status.humanize %>
        </span>
      </div>
      <div class="flex gap-2 ml-4">
        <% if @one_on_one.scheduled? %>
          <%= link_to 'Modifier', edit_manager_one_on_one_path(@one_on_one),
                class: 'inline-flex items-center py-1.5 px-3 border border-border-soft dark:border-white/15 rounded-md text-sm font-medium text-text-deep dark:text-white/70 bg-surface dark:bg-surface/10 hover:bg-bg-soft dark:hover:bg-gray-600 transition-all' %>
        <% end %>
        <%= button_to 'Supprimer', manager_one_on_one_path(@one_on_one),
              method: :delete,
              class: 'inline-flex items-center py-1.5 px-3 border border-transparent rounded-md text-sm font-medium text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 transition-all cursor-pointer',
              data: { confirm: 'Supprimer ce 1:1 ?' } %>
      </div>
    </div>

    <% if @one_on_one.agenda.present? %>
      <div class="mt-6">
        <h2 class="text-sm font-medium text-muted-soft dark:text-white/50 mb-1">Agenda</h2>
        <p class="text-text-deep dark:text-white whitespace-pre-wrap"><%= @one_on_one.agenda %></p>
      </div>
    <% end %>

    <% if @one_on_one.notes.present? %>
      <div class="mt-4">
        <h2 class="text-sm font-medium text-muted-soft dark:text-white/50 mb-1">Notes</h2>
        <p class="text-text-deep dark:text-white/70 whitespace-pre-wrap"><%= @one_on_one.notes %></p>
      </div>
    <% end %>
  </div>

  <!-- Clôture -->
  <% if @one_on_one.scheduled? %>
    <div class="bg-surface dark:bg-[#1C1B2E] rounded-lg shadow-sm border border-border-soft dark:border-white/10 p-6">
      <h2 class="text-lg font-semibold mb-4 text-text-deep dark:text-white">Clôturer ce 1:1</h2>
      <%= form_with url: complete_manager_one_on_one_path(@one_on_one), method: :patch do |f| %>
        <div class="mb-4">
          <%= label_tag :notes, 'Notes de séance', class: 'block text-sm font-medium text-text-deep dark:text-white/70 mb-1' %>
          <%= text_area_tag :notes, @one_on_one.notes, rows: 4,
                class: 'block w-full rounded-md border-border-soft dark:border-white/15 bg-surface dark:bg-surface/10 text-text-deep dark:text-white shadow-sm focus:border-primary/30 focus:ring-purple-500 text-sm',
                placeholder: 'Points abordés, décisions prises, actions à suivre…' %>
        </div>
        <%= f.submit 'Marquer comme complété',
              class: 'inline-flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-semibold text-white bg-primary/10 hover:bg-primary/10 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500 transition-all cursor-pointer' %>
      <% end %>
    </div>
  <% end %>
</div>
