<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
  <!-- Header -->
  <div class="mb-6">
    <h1 class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-gray-100 mb-2">Horaires de l'équipe</h1>
    <p class="text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500">Planifiez les horaires de travail de votre équipe semaine par semaine</p>
  </div>

  <!-- Stats Cards -->
  <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-6">
    <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-600 rounded-lg shadow p-6">
      <p class="text-sm font-medium text-gray-600 dark:text-gray-400 dark:text-gray-500">Membres d'équipe</p>
      <p class="text-3xl font-bold text-gray-900 dark:text-gray-100 mt-2"><%= @team_members.count %></p>
    </div>

    <div class="bg-gradient-to-br from-green-50 to-green-100 dark:from-green-900 dark:to-green-700 border-2 border-green-300 dark:border-green-500 rounded-lg p-6 shadow-md">
      <p class="text-sm font-medium text-green-800 dark:text-white opacity-90">Plannings créés</p>
      <p class="text-3xl font-bold text-green-900 dark:text-white mt-2"><%= @members_with_schedule.count %></p>
    </div>

    <div class="bg-gradient-to-br from-orange-50 to-orange-100 dark:from-orange-900 dark:to-orange-700 border-2 border-orange-300 dark:border-orange-500 rounded-lg p-6 shadow-md">
      <p class="text-sm font-medium text-orange-800 dark:text-white opacity-90">À planifier</p>
      <p class="text-3xl font-bold text-orange-900 dark:text-white mt-2"><%= @members_without_schedule.count %></p>
    </div>
  </div>

  <!-- Members Without Schedule (Priority) -->
  <% if @members_without_schedule.any? %>
    <div class="bg-orange-50 dark:bg-orange-900 border-2 border-orange-500 rounded-lg p-6 mb-6">
      <div class="flex items-center mb-4">
        <svg class="w-6 h-6 text-orange-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
        </svg>
        <h2 class="text-lg font-semibold text-orange-900">
          <%= @members_without_schedule.count %> employé<%= 's' if @members_without_schedule.count > 1 %> sans planning
        </h2>
      </div>

      <div class="space-y-3">
        <% @members_without_schedule.each do |member| %>
          <div class="bg-white dark:bg-gray-800 rounded-lg p-4 flex items-center justify-between">
            <div class="flex items-center">
              <div class="h-10 w-10 rounded-full bg-orange-100 dark:bg-orange-800 flex items-center justify-center mr-3">
                <span class="text-orange-700 dark:text-orange-300 font-semibold">
                  <%= member.first_name[0] %><%= member.last_name[0] %>
                </span>
              </div>
              <div>
                <p class="font-semibold text-gray-900 dark:text-gray-100"><%= member.full_name %></p>
                <p class="text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500">
                  <%= member.department || 'Équipe' %> • <%= member.contract_type %>
                </p>
              </div>
            </div>

            <%= link_to manager_team_member_weekly_schedule_plans_path(member),
              class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-orange-600 hover:bg-orange-700" do %>
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
              </svg>
              Planifier
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>

  <!-- Members With Schedule -->
  <div class="bg-white dark:bg-gray-800 rounded-lg shadow overflow-hidden">
    <div class="px-6 py-4 border-b border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900">
      <h2 class="text-lg font-semibold text-gray-900 dark:text-gray-100">Plannings configurés</h2>
    </div>

    <% if @members_with_schedule.any? %>
      <!-- Desktop Table View -->
      <div class="hidden sm:block overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-700">
          <thead class="bg-gray-50 dark:bg-gray-900">
            <tr>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 dark:text-gray-500 uppercase tracking-wider">Employé</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 dark:text-gray-500 uppercase tracking-wider">Contrat</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 dark:text-gray-500 uppercase tracking-wider">Semaines planifiées</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 dark:text-gray-500 uppercase tracking-wider">Actions</th>
            </tr>
          </thead>
          <tbody class="bg-white dark:bg-gray-800 divide-y divide-gray-200 dark:divide-gray-700">
            <% @members_with_schedule.each do |member| %>
              <tr class="hover:bg-gray-50 dark:bg-gray-900">
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="flex items-center">
                    <div class="h-10 w-10 rounded-full bg-indigo-100 dark:bg-indigo-800 flex items-center justify-center mr-3">
                      <span class="text-indigo-700 dark:text-indigo-300 font-semibold">
                        <%= member.first_name[0] %><%= member.last_name[0] %>
                      </span>
                    </div>
                    <div>
                      <p class="font-semibold text-gray-900 dark:text-gray-100"><%= member.full_name %></p>
                      <p class="text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500"><%= member.department || 'Équipe' %></p>
                    </div>
                  </div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <p class="text-sm font-medium text-gray-900 dark:text-gray-100"><%= member.contract_type %></p>
                  <% if member.work_schedule %>
                    <p class="text-xs text-gray-500 dark:text-gray-400 dark:text-gray-500"><%= member.work_schedule.weekly_hours %>h/semaine</p>
                  <% end %>
                </td>
                <td class="px-6 py-4">
                  <% upcoming_plans = member.weekly_schedule_plans.upcoming.limit(3) %>
                  <% if upcoming_plans.any? %>
                    <div class="text-sm space-y-1">
                      <% upcoming_plans.each do |plan| %>
                        <div class="flex items-center">
                          <% if plan.current_week? %>
                            <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-green-100 dark:bg-green-800 text-green-800 dark:text-green-200 mr-2">
                              Cette semaine
                            </span>
                          <% end %>
                          <span class="text-gray-900 dark:text-gray-100">
                            <%= l(plan.week_start_date, format: :short) %>
                            <span class="text-gray-500 dark:text-gray-400 dark:text-gray-500">(<%= plan.total_weekly_hours.round(1) %>h)</span>
                          </span>
                        </div>
                      <% end %>
                      <% if member.weekly_schedule_plans.upcoming.count > 3 %>
                        <p class="text-xs text-gray-500 dark:text-gray-400 dark:text-gray-500">
                          +<%= member.weekly_schedule_plans.upcoming.count - 3 %> autres...
                        </p>
                      <% end %>
                    </div>
                  <% else %>
                    <p class="text-sm text-gray-400 dark:text-gray-500">Aucune semaine à venir</p>
                  <% end %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm space-y-1">
                  <%= link_to manager_team_member_weekly_schedule_plans_path(member),
                    class: "block text-indigo-600 hover:text-indigo-900 font-medium" do %>
                    📅 Gérer les plannings
                  <% end %>
                  <% pending_validations = member.time_entries.pending_validation.count %>
                  <%= link_to manager_team_member_time_entries_path(member),
                    class: "block text-orange-600 hover:text-orange-900 font-medium" do %>
                    ⏰ Valider les heures
                    <% if pending_validations > 0 %>
                      <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-orange-100 dark:bg-orange-800 text-orange-800 dark:text-orange-200 ml-1">
                        <%= pending_validations %>
                      </span>
                    <% end %>
                  <% end %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>

      <!-- Mobile Card View -->
      <div class="sm:hidden divide-y divide-gray-200 dark:divide-gray-700">
        <% @members_with_schedule.each do |member| %>
          <div class="p-4">
            <div class="flex items-center justify-between mb-3">
              <div class="flex items-center">
                <div class="h-10 w-10 rounded-full bg-indigo-100 dark:bg-indigo-800 flex items-center justify-center mr-3">
                  <span class="text-indigo-700 dark:text-indigo-300 font-semibold">
                    <%= member.first_name[0] %><%= member.last_name[0] %>
                  </span>
                </div>
                <div>
                  <p class="font-semibold text-gray-900 dark:text-gray-100"><%= member.full_name %></p>
                  <p class="text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500"><%= member.contract_type %></p>
                </div>
              </div>
            </div>

            <div class="bg-gray-50 dark:bg-gray-900 rounded-lg p-3 mb-3">
              <% upcoming_plans = member.weekly_schedule_plans.upcoming.limit(2) %>
              <% if upcoming_plans.any? %>
                <div class="space-y-2">
                  <% upcoming_plans.each do |plan| %>
                    <div class="text-sm">
                      <%= l(plan.week_start_date, format: :short) %>
                      <span class="text-gray-600 dark:text-gray-400 dark:text-gray-500">(<%= plan.total_weekly_hours.round(1) %>h)</span>
                    </div>
                  <% end %>
                </div>
              <% else %>
                <p class="text-sm text-gray-500 dark:text-gray-400 dark:text-gray-500">Aucune semaine planifiée</p>
              <% end %>
            </div>

            <div class="space-y-2">
              <%= link_to manager_team_member_weekly_schedule_plans_path(member),
                class: "block w-full text-center px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md text-sm font-medium text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 hover:bg-gray-50 dark:bg-gray-900" do %>
                📅 Gérer les plannings
              <% end %>
              <% pending_validations = member.time_entries.pending_validation.count %>
              <%= link_to manager_team_member_time_entries_path(member),
                class: "block w-full text-center px-4 py-2 border border-orange-300 rounded-md text-sm font-medium text-orange-700 dark:text-orange-300 bg-white dark:bg-gray-800 hover:bg-orange-50 dark:bg-orange-900" do %>
                ⏰ Valider les heures
                <% if pending_validations > 0 %>
                  <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-orange-100 dark:bg-orange-800 text-orange-800 dark:text-orange-200 ml-1">
                    <%= pending_validations %>
                  </span>
                <% end %>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="p-12 text-center">
        <p class="text-gray-500 dark:text-gray-400 dark:text-gray-500">Aucun planning configuré pour le moment.</p>
      </div>
    <% end %>
  </div>

  <!-- Back Link -->
  <div class="mt-6">
    <%= link_to pending_approvals_leave_requests_path, class: "inline-flex items-center px-4 py-2 border border-gray-300 dark:border-gray-600 shadow-sm text-sm font-medium rounded-md text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 hover:bg-gray-50 dark:bg-gray-900" do %>
      <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
      </svg>
      Retour
    <% end %>
  </div>
</div>
